/*Creo userExtension para agregar metodos y atributos a User sin tener que crear
una clase que herede de User, ya que user es una clase proporcionada por Firebase*/

import 'package:firebase_auth/firebase_auth.dart';

extension userExtension on User{

}
